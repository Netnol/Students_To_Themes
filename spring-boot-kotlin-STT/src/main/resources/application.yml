spring:
  application:
    name: spring-boot-kotlin-STT

  datasource:
    url: ${DATABASE_URL}
    username: "postgres"
    password: ${POSTGRES_PASSWORD}
    driver-class-name: org.postgresql.Driver

server:
  port: ${PORT:8080}